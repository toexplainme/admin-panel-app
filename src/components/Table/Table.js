import TableBody from './TableBody';
import TableFooter from './TableFooter';

function Table() {
        return (
                <div className="table">
                        <TableBody/>
                        <TableFooter/>
                </div>
        );
}

export default Table;